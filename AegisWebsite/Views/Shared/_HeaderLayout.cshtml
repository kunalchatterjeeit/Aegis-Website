<div class="site-header header-v3">

    <!-- /.flat-top -->

    <header id="header" class="header clearfix">
        <div class="header-wrap clearfix">
            <div class="container">
                <div class="row">
                    <div class="flat-wrapper">
                        <div id="logo" class="logo">
                            <a href="@Url.Action("index", "Home")">
                                <img src="~/Contents/images/aegis_logo.webp" alt="images" style="height:70px">
                            </a>
                        </div>
                        <!-- /.logo -->
                        <div class="btn-menu">
                            <span></span>
                        </div>
                        <!-- //mobile menu button -->

                        <div class="nav-wrap">
                            <nav id="mainnav" class="mainnav">

                                <!-- /.menu-extra -->
                                <ul class="menu">
                                    <li @(HttpContext.Current.Request.RequestContext.RouteData.Values["action"].ToString().Equals("Index") ? "class=home" : "")><a href="@Url.Action("index", "Home")">Home</a></li>
                                    <li @(HttpContext.Current.Request.RequestContext.RouteData.Values["action"].ToString().Equals("About-us") ? "class=home" : "")><a href="@Url.Action("about-us", "Home")">About Us</a></li>
                                    <li @(HttpContext.Current.Request.RequestContext.RouteData.Values["action"].ToString().Equals("Document-Managment-Solutions-with-workflow") || HttpContext.Current.Request.RequestContext.RouteData.Values["action"].ToString().Equals("Scanning-and-Digitization-Services") ? "class=home" : "")>
                                        <a href="#">Document Management Solutions</a>
                                        <ul class="submenu">
                                            <li><a href="@Url.Action("document-managment-solutions-with-workflow", "Home")">DMS software with workflow</a></li>
                                            <li><a href="@Url.Action("scanning-and-digitization-services", "Home")">Scanning & Digitization Services</a></li>
                                        </ul>
                                        <!-- /.submenu -->
                                    </li>
                                    <li @(HttpContext.Current.Request.RequestContext.RouteData.Values["action"].ToString().Equals("Manage-Print-Solutions") ? "class=home" : "")>
                                        <a href="#">Manage Print Solutions</a>
                                        <ul class="submenu">
                                            <li><a href="@Url.Action("manage-print-solutions", "Home")">Copier, printer on Rent Solutions</a></li>
                                        </ul>
                                        <!-- /.submenu -->
                                    </li>
                                    <li @(HttpContext.Current.Request.RequestContext.RouteData.Values["action"].ToString().Equals("Contact-us") ? "class=home" : "")><a href="@Url.Action("contact-us", "Home")">Contact</a></li>
                                </ul>
                                <!-- /.menu -->
                            </nav>
                            <!-- /.mainnav -->
                        </div>
                        <!-- /.nav-wrap -->
                    </div>
                    <!-- /.flat-wrapper -->
                </div>
                <!-- /.row -->
            </div>
            <!-- /.container -->
        </div>
        <!-- /.header-inner -->
    </header>
    <!-- /.header -->
</div>